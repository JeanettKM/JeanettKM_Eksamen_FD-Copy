import React from "react";
import { useNavigate } from "react-router-dom";
import Button from "react-bootstrap/Button";
import Card from "react-bootstrap/Card";
import ListGroup from "react-bootstrap/ListGroup";

function VenueCard({ venue }) {
  const navigate = useNavigate();
  
  // URL of the first image of the venue, or a default image URL if not available
  const firstImageUrl = venue.media?.[0]?.url ?? "default-image-url";
  // Alt text of the first image of the venue, or a default alt text if not available
  const firstImageAlt = venue.media?.[0]?.alt ?? "Default alt text";

  // Function to navigate to the detailed page of the venue
  const handleReadMore = () => {
    navigate(`/venues/${venue.id}`);
  };

  return (
    <Card className="venue-card">
      {/* Display the first image of the venue */}
      <Card.Img
        variant="top"
        src={firstImageUrl}
        alt={firstImageAlt}
        className="card-img-top"
      />
      <Card.Body style={{ height: "100px", overflow: "hidden" }}>
        {/* Display the name of the venue */}
        <Card.Title>{venue.name}</Card.Title>
        {/* Display the description of the venue */}
        <Card.Text className="card-text">{venue.description}</Card.Text>
      </Card.Body>
      {/* Display venue details in a list */}
      <ListGroup className="list-group-flush" style={{ height: "150px", overflow: "hidden" }}>
        <ListGroup.Item>
          Location: {venue.location.city}, {venue.location.country}
        </ListGroup.Item>
        <ListGroup.Item>Rated: {venue.rating}</ListGroup.Item>
        <ListGroup.Item>Price: {venue.price}</ListGroup.Item>
      </ListGroup>
      <Card.Body>
        {/* Button to navigate to the detailed page of the venue */}
        <Button variant="primary" onClick={handleReadMore}>
          Read More
        </Button>
      </Card.Body>
    </Card>
  );
}

export default VenueCard;
